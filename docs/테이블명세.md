## `users` - 계정 기본 정보

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| email | varchar(255) | N | 로그인 ID<br>UK |
| password | varchar(255) | Y | BCrypt 해시. 소셜 전용 가입자는 null |
| nickname | varchar(50) | N | 표시명<br>UK |
| introduction | varchar(255) | Y | 자기소개 / 마이페이지 프로필에 표시 |
| role | **enum** varchar(20) | N | `USER` / `ADMIN` |
| personal_streak | int | N | 참여 중인 전 챌린지를 합쳐서 본 연속 성공일 |
| best_streak | int | N | 개인 최고 연속 기록 |
| last_checked_in_date | date | Y | 마지막 인증한 논리적 날짜 |
| email_verified | boolean | N | 이메일 소유 확인 여부. 기본값 false |
| deleted_at | datetime(6) | Y | 소프트 딜리트 시각<br>탈퇴 시 `email`,`nickname`을 `deleted_{id}`로 치환. 같은 이메일로 재가입 가능 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_users_email(email)` - 이메일 중복 가입 방지
**UK:** `uk_users_nickname(nickname)` - 닉네임 중복 방지

---

## `refresh_tokens` - 리프레시 토큰

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users |
| token_hash | varchar(255) | N | 토큰 해시값. 원문 저장 금지. UK |
| expires_at | datetime(6) | N | 만료 시각 |
| rotated_at | datetime(6) | Y | 로테이션으로 폐기된 시각 |
| revoked_at | datetime(6) | Y | 그 외 사유로 폐기된 시각. 둘 다 null이면 유효 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_refresh_tokens_hash(token_hash)` - 같은 토큰 중복 저장 방지
**인덱스:** `idx_refresh_tokens_user(user_id, revoked_at)` - 유저별 유효 토큰 조회, 재사용 탐지 시 전체 폐기

---

## `challenge_groups` - 챌린지 그룹

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| name | varchar(100) | N | 그룹명 겸 챌린지명. 모든 시즌이 공유 |
| description | text | Y | 상세 설명. 모든 시즌이 공유 |
| category | **enum** varchar(20) | N | `DEV` / `READING` / `JOB` / `STUDY` / `EXERCISE` / `HEALTH` / `LIFE` / `ETC` |
| map_type | **enum** varchar(20) | N | `STUDY_ROOM` / `GYM`. 카테고리로 결정 |
| invite_code | varchar(6) | Y | 비공개(CODE_ONLY) 그룹 초대 코드. UK |
| visibility | **enum** varchar(20) | N | `CODE_ONLY` / `PUBLIC` |
| max_members | int | N | 정원 1~6(코드단 검증) |
| owner_id | bigint | N | FK → users. 방장 |
| status | **enum** varchar(20) | N | `READY` / `ACTIVE` / `ENDED` |
| kick_vote_started_at | datetime(6) | Y | 방장 강퇴 투표 시작 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_challenge_groups_invite_code(invite_code)` - 초대 코드 중복 방지
**인덱스:** `idx_groups_category_status(category, status)` - 공개방 목록 카테고리 필터링
**인덱스:** `idx_challenge_groups_kick_vote_started_at(kick_vote_started_at)` - 진행 중인 강퇴 투표 조회
**인덱스(자동):** `fk_challenge_groups_owner(owner_id)`

---

## `group_members` - 그룹 회원

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups |
| user_id | bigint | N | FK → users |
| status | **enum** varchar(20) | N | 멤버 상태 |
| left_at | datetime(6) | Y | 이탈 시각 |
| last_read_message_id | bigint | Y | FK → group_messages. 마지막으로 읽은 메시지 |
| kick_vote_choice | varchar(20) | N | 방장 강퇴 투표 선택. 기본값 `NONE` |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_group_members(group_id, user_id)` - 중복 가입 방지
**인덱스:** `idx_group_members_user(user_id, status)` - 내가 속한 그룹 목록 조회
**인덱스(자동):** `fk_group_members_last_read_message(last_read_message_id)`

---

## `challenges` - 챌린지 한 시즌

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups |
| seq_no | int | N | 시즌 번호(1, 2, 3...) |
| start_date | date | N | 시작일 |
| end_date | date | N | 종료일 |
| status | **enum** varchar(20) | N | `READY` / `ACTIVE` / `ENDED` |
| frequency_type | **enum** varchar(20) | N | `DAILY` / `DAYS_OF_WEEK` / `EVERY_N_DAYS` |
| frequency_value | int | Y | `EVERY_N_DAYS`의 N |
| days_of_week | varchar(30) | Y | `DAYS_OF_WEEK`일 때 `"MON,WED,FRI"` |
| daily_check_in_count | int | N | 하루 인증 횟수 |
| required_day_count | int | N | 전체 기간 중 인증해야 하는 날의 총 개수(완료율 분모) |
| group_current_streak | int | N | 참여자 전원이 하루치를 다 채운 날의 현재 연속 개수 |
| group_best_streak | int | N | 연속 최고 기록 |
| allow_photo | boolean | N | 사진 인증 허용 |
| allow_video | boolean | N | 영상 인증 허용. 기본값 false |
| group_last_completed_date | date | Y | ACTIVE 멤버 전원이 마지막으로 당일 목표를 채운 날 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_challenges_group_seq(group_id, seq_no)` - 시즌 번호 중복 방지
**인덱스:** `idx_challenges_status_end(status, end_date)` - 종료일 도래 챌린지 조회, 연장 마감 판정

---

## `challenge_members` - 챌린지 시즌별 개인 현재 통계

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| challenge_id | bigint | N | FK → challenges |
| user_id | bigint | N | FK → users |
| role | **enum** varchar(20) | N | `OWNER` / `MEMBER` |
| status | **enum** varchar(20) | N | `ACTIVE` / `LEFT` / `KICKED` |
| current_streak | int | N | 이 챌린지 기준 현재 연속 인증일 |
| best_streak | int | N | 이 챌린지 최고 연속 기록 |
| extension_choice | **enum** varchar(20) | N | `PENDING` / `EXTEND` / `DECLINE` |
| left_at | datetime(6) | Y | 이탈 시각 |
| last_completed_date | date | Y | 개인이 그 시즌에서 마지막으로 당일 목표를 채운 날 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_challenge_members(challenge_id, user_id)` - 중복 참여 방지
**인덱스:** `idx_challenge_members_user(user_id, status)` - 내가 참여 중인 챌린지 목록 조회

---

## `check_ins` - 개인의 인증 1건

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| challenge_id | bigint | N | FK → challenges |
| user_id | bigint | N | FK → users |
| round_no | int | N | 그날 몇 번째 인증인가(1부터), 매일 리셋 |
| check_in_type | **enum** varchar(20) | N | `PHOTO` / `VIDEO` / `LIVE` |
| media_key | varchar(255) | N | 스토리지 키 |
| media_type | **enum** varchar(20) | N | `IMAGE` / `VIDEO` |
| memo | varchar(100) | Y | 인증 시 남기는 한 줄 메모 |
| business_date | date | N | 4시 기준 논리적 날짜 |
| poster_key | varchar(255) | Y | 영상 체크인 그리드 표시용 썸네일 스토리지 키. 이미지 체크인은 null |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_check_ins(challenge_id, user_id, business_date, round_no)` - 중복 인증 방지
**인덱스:** `idx_check_ins_daily(challenge_id, business_date)` - 챌린지 하루치 인증 조회, 일일로그 합성, 그룹 갤러리
**인덱스:** `idx_check_ins_user(user_id, business_date)` - 잔디, 개인 종합 스트릭

---

## `daily_logs` - 일일로그

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| challenge_id | bigint | N | FK → challenges |
| log_date | date | N | 이 로그가 어느 날짜의 것인지 |
| video_key | varchar(255) | Y | 스토리지 키 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_daily_logs(challenge_id, log_date)` - 하루 한 개

---

## `user_point_histories` - 개인 포인트 변동 내역

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users |
| challenge_id | bigint | Y | FK → challenges. 챌린지와 연관없으면 null |
| source_name | varchar(100) | N | 발생 경로 표시명(챌린지명 or 아이템명 스냅샷) |
| amount | int | N | 지급 `+`, 차감 `-` |
| reason | **enum** varchar(30) | N | `CHECK_IN` / `CHALLENGE_BONUS` / `MONTHLY_MERGE_BONUS` / `ITEM_PURCHASE` / `WITHDRAWAL_PENALTY` |
| balance_after | int | N | 변동 발생 직후 잔액 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_user_point_histories_user(user_id, created_at)` - 기간 필터 조회
**인덱스:** `idx_user_point_histories_user_cursor(user_id, id)` - 최신순 커서 페이징, 잔액 SUM
**인덱스(자동):** `fk_user_point_histories_challenge(challenge_id)`

---

## `group_point_histories` - 그룹 포인트 변동 내역

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups |
| source_name | varchar(100) | N | 발생 경로 표시명(그룹명 or 배경 아이템명 스냅샷) |
| amount | int | N | 지급 `+`, 차감 `-` |
| reason | **enum** varchar(30) | N | `DAILY_ALL_COMPLETE` / `MERGE_BONUS` / `BACKGROUND_PURCHASE` |
| balance_after | int | N | 변동 발생 직후 잔액 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_group_point_histories_group(group_id, created_at)` - 기간 필터 조회
**인덱스:** `idx_group_point_histories_group_cursor(group_id, id)` - 최신순 커서 페이징, 잔액 SUM

---

## `user_points` - 개인 포인트 잔액

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users. UK — 유저당 1행 |
| balance | int | N | 잔액. `DEFAULT 0` |
| pending_deduction | int | N | 챌린지 중도 탈퇴 회수 중 못 깎고 남은 금액. `DEFAULT 0` |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_user_points_user(user_id)`

---

## `group_points` - 그룹 포인트 잔액

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups. UK — 그룹당 1행 |
| balance | int | N | 잔액. `DEFAULT 0` |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_group_points_group(group_id)`

---

## `items` - 상점 판매 아이템

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| slot | **enum** varchar(20) | N | 장착 위치. `HEAD` / `TOP` / `BOTTOM` / `SHOES` |
| name | varchar(50) | N | 아이템 표시명 |
| price | int | N | 포인트 소모 비용 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

---

## `item_images` - 아이템 포즈별 이미지

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| item_id | bigint | N | FK → items |
| pose | varchar(20) | N | 포즈 구분 |
| image_key | varchar(255) | N | 스토리지 키 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_item_images_item_pose(item_id, pose)` - 아이템당 포즈 하나에 이미지 하나

---

## `user_items` - 유저 아이템 상태

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users |
| item_id | bigint | N | FK → items |
| equipped_slot | **enum** varchar(20) | Y | `HEAD` / `TOP` / `BOTTOM` / `SHOES`, 보유만 하면 null |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_user_items(user_id, item_id)` - 중복 구매 방지
**UK:** `uk_user_items_equipped(user_id, equipped_slot)` - 슬롯당 하나만 착용
**인덱스(자동):** `fk_user_items_item(item_id)`

---

## `backgrounds` - 상점 판매 배경

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| map_type | **enum** varchar(20) | N | `STUDY_ROOM` / `GYM`. 이 배경이 속한 계열. 같은 계열 그룹만 구매 가능 |
| name | varchar(50) | N | 배경 표시명 |
| image_key | varchar(255) | N | 스토리지 키 |
| price | int | N | 가격 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

인덱스 없음.

---

## `group_backgrounds` - 그룹이 보유한 배경

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups |
| background_id | bigint | N | FK → backgrounds |
| status | **enum** varchar(20) | N | `INACTIVE`(보유만) / `ACTIVE`(그룹 화면에 걸림) |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_group_backgrounds(group_id, background_id)` - 중복 구매 방지
**인덱스(자동):** `fk_group_backgrounds_background(background_id)`

---

## `background_purchase_requests` - 배경 구매 제안

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups |
| background_id | bigint | N | FK → backgrounds |
| requested_by | bigint | N | FK → users. 제안자 |
| status | **enum** varchar(20) | N | `VOTING` / `APPROVED` / `REJECTED` |
| expires_at | datetime(6) | N | 제안 시각 + 3일 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_background_purchase_requests(group_id, status)` - 그룹의 진행 중 제안 조회
**인덱스(자동):** `fk_background_purchase_requests_background(background_id)`
**인덱스(자동):** `fk_background_purchase_requests_user(requested_by)`

---

## `background_purchase_votes` - 제안에 대한 그룹원 투표

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| request_id | bigint | N | FK → background_purchase_requests |
| user_id | bigint | N | FK → users |
| agreed | boolean | N | 찬성 여부 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_background_purchase_votes(request_id, user_id)` - 1인 1표
**인덱스(자동):** `fk_background_purchase_votes_user(user_id)`

---

## `reports` - 신고

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| reporter_id | bigint | N | FK → users. 신고자. 직권 제재면 관리자 본인 |
| target_type | **enum** varchar(20) | N | `USER` / `CHECK_IN` / `CHAT_MESSAGE` |
| target_id | bigint | N | FK 아님. 대상 종류에 따라 가리키는 테이블이 다름 |
| target_user_id | bigint | N | FK → users. 제재 대상. 접수 시점에 확정 |
| reason | **enum** varchar(20) | N | `ABUSE` / `SPAM` / `SEXUAL` / `FAKE` / `ETC` |
| reported_content | varchar(1000) | Y | 접수 시점 원본 스냅샷 |
| detail | varchar(500) | Y | 신고자 자유 서술 |
| status | **enum** varchar(20) | N | `PENDING` / `ACCEPTED` / `REJECTED` |
| decided_by | bigint | Y | FK → users. 판정한 관리자 |
| decided_at | datetime(6) | Y | 판정 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_reports_status(status, id)` - 관리자 목록 커서 페이징
**인덱스:** `idx_reports_reporter_target(reporter_id, target_type, target_id)` - 중복 접수 확인
**인덱스:** `idx_reports_target(target_type, target_id)` - 같은 대상의 신고 모아보기

---

## `user_penalties` - 제재

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| report_id | bigint | N | FK → reports. 직권 제재도 신고 행을 만들어 붙임 |
| user_id | bigint | N | FK → users. 제재 대상 |
| penalty_type | **enum** varchar(20) | N | `WARNING` / `SUSPENSION` / `PERMANENT_BAN` / `POINT_FORFEIT` |
| ends_at | datetime(6) | Y | 기간 정지 종료 시각. 영구 정지는 null |
| amount | int | Y | 포인트 압수액 |
| revoked_at | datetime(6) | Y | 이의제기 인용 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_user_penalties_active(user_id, revoked_at, ends_at)` - 로그인 시 유효 제재 조회

---

## `report_appeals` - 이의제기

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| report_id | bigint | N | FK → reports. UK — 신고 1건당 이의제기 1회 |
| appellant_id | bigint | N | FK → users. 제재 대상 본인 |
| content | varchar(1000) | N | 이의제기 내용 |
| status | **enum** varchar(20) | N | `PENDING` / `ACCEPTED` / `REJECTED` |
| decided_by | bigint | Y | FK → users. 판정한 관리자 |
| decided_at | datetime(6) | Y | 판정 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_report_appeals_report(report_id)`
**인덱스:** `idx_report_appeals_status(status, id)` - 관리자 목록 커서 페이징

---

## `group_messages` - 그룹 채팅 메시지

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| group_id | bigint | N | FK → challenge_groups |
| sender_id | bigint | Y | FK → users. 시스템 메시지면 null |
| message_type | **enum** varchar(20) | N | `TEXT` / `SYSTEM` |
| content | varchar(1000) | N | 메시지 본문 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) = 발송 시각 |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_group_messages_group_cursor(group_id, id)` - 그룹 대화 최신순 커서 페이징
**인덱스(자동):** `fk_group_messages_user(sender_id)`

---

## `notifications` - 알림 이력

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users |
| type | varchar(30) | N | 알림 종류 |
| title | varchar(100) | N | 알림 제목 |
| body | varchar(255) | Y | 알림 본문 |
| ref_id | bigint | Y | 알림이 가리키는 대상 id. 종류에 따라 가리키는 테이블이 다름 |
| read_at | datetime(6) | Y | 읽은 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**인덱스:** `idx_notifications_user(user_id, read_at, created_at)` - 유저별 알림 목록, 안 읽은 알림 조회

---

## `auth_identities` - 계정에 연결된 로그인 수단

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users |
| provider | **enum** varchar(20) | N | `LOCAL` / `GOOGLE` / `NAVER`. 실제로 쓰는 값은 `GOOGLE` · `NAVER` 뿐 |
| provider_user_id | varchar(255) | N | 프로바이더가 발급한 사용자 식별자 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_auth_identities(provider, provider_user_id)` - 같은 소셜 계정이 두 사람에게 붙는 것 방지
**UK:** `uk_auth_identities_user_provider(user_id, provider)` - 한 사람이 같은 프로바이더 계정을 여러 개 붙이는 것 방지
**인덱스:** `idx_auth_identities_user(user_id)` - 이 사용자의 연결 수단 목록 조회

---

## `email_tokens` - 가입 인증 · 비밀번호 재설정

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| user_id | bigint | N | FK → users |
| token_hash | varchar(255) | N | 메일 링크 토큰의 해시값. 원문 저장 금지. UK |
| token_type | **enum** varchar(20) | N | `SIGNUP_VERIFY` / `PASSWORD_RESET` |
| expires_at | datetime(6) | N | 만료 시각 |
| used_at | datetime(6) | Y | 사용 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_email_tokens_hash(token_hash)` - 토큰 중복 저장 방지
**인덱스:** `idx_email_tokens_user(user_id, token_type)` - 유저별 유효 토큰 조회

---

## `monthly_merges` - 챌린지 진행 중 30일마다 발행되는 그룹 단위 체크포인트

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| challenge_id | bigint | N | FK → challenges |
| seq_no | int | N | 발행 회차 |
| period_start | date | N | 집계 시작일 |
| period_end | date | N | 집계 종료일 |
| total_days | int | N | 집계 기간 일수 |
| total_check_in_count | int | N | 기간 내 전체 인증 횟수 |
| average_completion_rate | int | N | 참여자 평균 완료율 |
| published_at | datetime(6) | N | 발행 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_monthly_merges_challenge_seq(challenge_id, seq_no)` - 챌린지당 회차 중복 방지

---

## `monthly_merge_results` - 월간 머지 1건에 대한 참여자별 결과

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| monthly_merge_id | bigint | N | FK → monthly_merges |
| user_id | bigint | N | FK → users |
| ranking | int | N | 순위 |
| completion_rate | int | N | 완료율 |
| completed_day_count | int | N | 인증한 날 수(완료율의 분자) |
| total_check_in_count | int | N | 기간 내 인증 횟수. 하루 여러 번 인증 시 기간 일수를 넘을 수 있음 |
| best_streak_in_period | int | N | 기간 내 최고 연속 인증일 |
| earned_points | int | N | 기간 내 획득 포인트 |
| contribution_rate | int | N | 그룹 성과 기여율 |
| check_in_trend_labels | varchar(100) | Y | 주간 인증 추이 라벨(`"1주,2주,3주,4주"`) |
| check_in_trend_counts | varchar(100) | Y | 주간 인증 추이 값(`"7,6,7,6"`) |
| head_image_url | varchar(255) | Y | 발행 시점 캐릭터 머리 이미지 |
| top_image_url | varchar(255) | Y | 발행 시점 캐릭터 상의 이미지 |
| bottom_image_url | varchar(255) | Y | 발행 시점 캐릭터 하의 이미지 |
| shoes_image_url | varchar(255) | Y | 발행 시점 캐릭터 신발 이미지 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_monthly_merge_results(monthly_merge_id, user_id)` - 머지당 유저 1행
**인덱스(자동):** `fk_monthly_merge_results_user(user_id)`

---

## `final_merges` - 챌린지 종료 시 1회 발행되는 최종 결산

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| challenge_id | bigint | N | FK → challenges. UK — 챌린지당 1행 |
| period_start | date | N | 집계 시작일 |
| period_end | date | N | 집계 종료일 |
| total_days | int | N | 집계 기간 일수 |
| total_check_in_count | int | N | 기간 내 전체 인증 횟수 |
| average_completion_rate | int | N | 참여자 평균 완료율 |
| published_at | datetime(6) | N | 발행 시각 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_final_merges_challenge(challenge_id)`

---

## `final_merge_results` - 최종 머지 1건에 대한 참여자별 결과

| 컬럼 | 타입 | NULL | 설명 |
| --- | --- | --- | --- |
| id | bigint | N | PK (BaseEntity) |
| final_merge_id | bigint | N | FK → final_merges |
| user_id | bigint | N | FK → users |
| ranking | int | N | 순위 |
| completion_rate | int | N | 완료율 |
| completed_day_count | int | N | 인증한 날 수(완료율의 분자) |
| total_check_in_count | int | N | 전체 기간 인증 횟수 |
| best_streak_in_period | int | N | 전체 기간 최고 연속 인증일 |
| earned_points | int | N | 전체 기간 획득 포인트 |
| contribution_rate | int | N | 그룹 성과 기여율 |
| check_in_trend_labels | varchar(200) | Y | 월별 인증 추이 라벨 |
| check_in_trend_counts | varchar(200) | Y | 월별 인증 추이 값 |
| head_image_url | varchar(255) | Y | 발행 시점 캐릭터 머리 이미지 |
| top_image_url | varchar(255) | Y | 발행 시점 캐릭터 상의 이미지 |
| bottom_image_url | varchar(255) | Y | 발행 시점 캐릭터 하의 이미지 |
| shoes_image_url | varchar(255) | Y | 발행 시점 캐릭터 신발 이미지 |
| created_at | datetime(6) | N | 생성 시각 (BaseEntity) |
| updated_at | datetime(6) | N | 수정 시각 (BaseEntity) |

**UK:** `uk_final_merge_results(final_merge_id, user_id)` - 머지당 유저 1행
**인덱스(자동):** `fk_final_merge_results_user(user_id)`
