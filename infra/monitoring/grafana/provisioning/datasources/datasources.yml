# Q29. Grafana 부팅 시 자동 등록 — 대시보드 만들 때 UI에서 datasource 따로 안 만들어도 됨.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
